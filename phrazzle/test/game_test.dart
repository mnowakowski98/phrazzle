import 'package:flutter_test/flutter_test.dart';

void main() {
  group('', () {
    test('Displays starting phrase if starting phrase is set', () {});
    test(
      'Doesn\'t display starting phrase if starting phrase is not set',
      () {},
    );
  });
}
